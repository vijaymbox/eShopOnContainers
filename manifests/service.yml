apiVersion: v1
kind: Service
metadata:
    name: "myakscluster-341b"
    labels:
        app: "myakscluster-341b"
spec:
    type: LoadBalancer
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "myakscluster-341b"